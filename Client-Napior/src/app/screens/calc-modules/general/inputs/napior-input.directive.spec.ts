import { NapiorInputDirective } from './napior-input.directive';

describe('NapiorInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NapiorInputDirective();
    expect(directive).toBeTruthy();
  });
});
