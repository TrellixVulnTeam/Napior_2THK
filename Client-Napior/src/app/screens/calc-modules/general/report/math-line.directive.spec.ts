import { MathLineDirective } from './math-line.directive';

describe('MathLineDirective', () => {
  it('should create an instance', () => {
    const directive = new MathLineDirective();
    expect(directive).toBeTruthy();
  });
});
